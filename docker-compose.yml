services:
  scanner-rtl1:
    build:
      context: .
      dockerfile: docker/scanner.Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      RTL_PREFIX: RTL1
    volumes:
      - ./data:/data
      - /dev/bus/usb:/dev/bus/usb
    privileged: true

  scanner-rtl2:
    build:
      context: .
      dockerfile: docker/scanner.Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      RTL_PREFIX: RTL2
    volumes:
      - ./data:/data
      - /dev/bus/usb:/dev/bus/usb
    privileged: true

  heatmap:
    build:
      context: .
      dockerfile: docker/python.Dockerfile
    restart: unless-stopped
    env_file: .env
    command: ["python", "-u", "/app/heatmap_worker.py"]
    volumes:
      - ./data:/data

  web:
    build:
      context: .
      dockerfile: docker/python.Dockerfile
    restart: unless-stopped
    env_file: .env
    command: ["python", "-u", "/app/gallery.py"]
    ports:
      - "${WEB_PORT:-8080}:8080"
    volumes:
      - ./data:/data:ro
